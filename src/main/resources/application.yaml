spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/security_db_v3
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update